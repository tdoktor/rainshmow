#include <stdio.h>
#include <stdbool.h>

int main(int argc, char *argv[])
{
  bool example = true;
  const int NUMBER = 1; 
  if (example) {
    printf("string %d", NUMBER);
  }
  // comment
  return 0;
}
